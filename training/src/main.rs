/// Hive AI training CLI.
///
/// This binary runs locally (not deployed to the web).
/// It trains the AI through self-play and generates opening books.

fn main() {
    println!("Hive AI Training Tool");
    println!("=====================");
    println!("TODO: Implement self-play RL and opening book generation.");
    println!("For now, the engine uses default evaluation weights.");
}
